---
title: "R Notebook"
output: html_notebook
---

```{r}
require(lol)
require(ggplot2)
require(latex2exp)
require(MASS)
require(gridExtra)
local_path <- '~/Documents/research/R-fmri/lol'  # local path to the package

funcs <- list(lol.project.pca, lol.project.cpca, lol.project.lrcca,
              lol.project.lol)
alg_names <- c("PCA", "PCA'", "LR-CCA", "LOL")
```

# MNIST Experiments

```{r}

```

# Colon Experiments

The colon data can be found by downloading the following links:

```
wget https://raw.githubusercontent.com/neurodata-papers/LOL/master/Data/Raw/colon/X.txt
wget https://raw.githubusercontent.com/neurodata-papers/LOL/master/Data/Raw/colon/Y.txt
```

and has 2000 features with 62 samples, with each line structured as:

```
feature1_samp1 feature1_samp2 feature1_samp3 ...
feature2_samp1 feature2_samp2 feature2_samp3 ...
```

```{r}
path_colx <- file.path(local_path, 'docs/lol-paper/data/colon/X.txt')
path_coly <- file.path(local_path, 'docs/lol-paper/data/colon/Y.txt')
Xcol <- t(read.table(path_colx, sep="", header=FALSE, na.strings="", stringsAsFactors = FALSE))  # load in the data
Ycol <- as.vector(read.table(path_coly, sep="", header=FALSE, na.strings="", stringsAsFactors = FALSE))[,1]  # load in the labels as a 62-element vector
Ycol <- as.numeric(Ycol > 0)  # binarize for 2-class

res_plots <- list()
results <- data.frame(alg=c(), r=c(), lhat=c())
for (i in 1:length(funcs)) {
  for (r in 1:20) {
    func_res <- lol.xval.eval(Xcol, Ycol, r, alg=funcs[[i]])
    results <- rbind(results, data.frame(alg=alg_names[i], r=r, lhat=func_res$Lhats))
  }
}
```

# MRI Experiments

